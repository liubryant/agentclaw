<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.AgentClaw" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- 全局禁用 Android 8.0 (API 26) 以上的默认高亮边框 -->
        <item name="android:defaultFocusHighlightEnabled" tools:targetApi="o">false</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="colorPrimary">@color/brand_primary</item>
        <item name="android:colorBackground">@color/black</item>
        <item name="android:windowBackground">@color/black</item>
    </style>

    <style name="Theme.AgentClaw" parent="Base.Theme.AgentClaw" />

    <style name="Theme.AgentClaw.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/black</item>
<!--        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher</item>-->
        <item name="postSplashScreenTheme">@style/Theme.AgentClaw</item>
    </style>

    <style name="Theme.AgentClaw.Transparent" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:colorBackground">@android:color/transparent</item>
    </style>

    <style name="Theme.AgentClaw.SearchOverlay" parent="Theme.AgentClaw.Transparent">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>
</resources>
